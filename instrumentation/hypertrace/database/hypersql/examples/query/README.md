# hyperSQL query example

Before running the `main.go` file make sure you have a MySQL database up and running with the following command:

```bash
docker run -e MYSQL_ROOT_PASSWORD=root -p "3306:3306" mysql
```
